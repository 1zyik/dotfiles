# Bash Prompt Style
1. Install your terminal of choice
2. Install powerline-fonts refer to https://github.com/powerline/fonts
3. Copy contents in `bash-prompt.sh` to `.bashrc`
4. Make root shell look the same by editing  `/root/.bashrc`
5. Restart terminal
